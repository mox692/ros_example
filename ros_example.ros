#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -S `pwd`/foo/ -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:foo) :silent t)
  )

(defpackage :ros.script.ros_example.3860270964
  (:use :cl))
(in-package :ros.script.ros_example.3860270964)

(defun main (&rest argv)
  (declare (ignorable argv))
  (foo:hellofoo))
;;; vim: set ft=lisp lisp:
