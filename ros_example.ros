#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -S `pwd`/my_system/ -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:my_system) :silent t)
  )

(defpackage :ros.script.ros_example.3860270964
  (:use :cl))
(in-package :ros.script.ros_example.3860270964)

(defun main (&rest argv)
  (declare (ignorable argv))
  (my_system:hellofoo)
  (my_system:hellofoofoo))
;;; vim: set ft=lisp lisp:
